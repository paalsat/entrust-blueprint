[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "entrust-blueprint"
authors = [{name = "Pål Sætrom", email = "paalsat@gmail.com"}]
dynamic = ["version", "description"]
